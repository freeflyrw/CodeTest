As a manipulator user, I want an endpoint on the Manipulator service 
that will reverse a string so that I can easily reverse text.

Acceptance Criteria
The endpoint should accept a string and return the reversed string
If the string is over 1024 characters long a suitable error should be returned